name: Manual Trigger Workflow

on:
  workflow_dispatch:  # Required to allow manual/API trigger

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run Tests
        run: echo "Running tests..."
